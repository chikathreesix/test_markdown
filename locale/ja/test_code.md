# タイトルル

```js
const Nuxt = require('nuxt')
const nuxt = new Nuxt()
//保ゲホゲホゲホゲホゲテストテストテスト
nuxt.renderAndGetWindow('http://localhost:3000')
.then((window) => {
  // ヘッド<title>を表示
  console.log(window.document.title)
})</title>
```

Test
